[build]
builder = "nixpacks"
buildCommand = "cd backend && pip install -r requirements-combined.txt"

[deploy]
startCommand = "cd backend && uvicorn src.api.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
